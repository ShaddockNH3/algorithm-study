[206. 反转链表 - 力扣（LeetCode）](https://leetcode.cn/problems/reverse-linked-list/)

O(n)空间版本：

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        if(head==nullptr||head->next==nullptr){
            return head;
        }
        ListNode* curr=head;

        ListNode* reverseNode=new ListNode(head->val,nullptr);

        while(curr->next!=nullptr){
            curr=curr->next;
            ListNode* reversenextNode=new ListNode(curr->val,reverseNode);
            reverseNode=reversenextNode;
        }
        
        return reverseNode;
    }
};
```

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode* temp;
        ListNode* pred=nullptr;
        ListNode* curr=head;

        while(curr!=nullptr){
            temp=curr->next;
            curr->next=pred;
            pred=curr;
            curr=temp;
        }

        return pred;

    }
};
```

这道题我刚开始是使用了O(n)的空间，后面发现实际上可以使用三个指针的方式。