```
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    bool isSubtree(TreeNode* root, TreeNode* subRoot) {
        if(root==nullptr&&subRoot==nullptr){
            return true;
        }else if(root==nullptr&&subRoot!=nullptr){
            return false;
        }
        queue<TreeNode*> q;
        q.push(root);
        for(;!q.empty();){
            int curr_length=q.size();
            for(int i=0;i<curr_length;i++){
                auto curr=q.front();
                q.pop();
                if(isSameTree(curr,subRoot)){
                    return true;
                }
                if(curr->left!=nullptr){
                    q.push(curr->left);
                }
                if(curr->right!=nullptr){
                    q.push(curr->right);
                }
            }
        }
        return false;
    }
    bool isSameTree(TreeNode* p, TreeNode* q) {
        if(p==nullptr&&q==nullptr){
            return true;
        }
        if(p==nullptr||q==nullptr||p->val!=q->val){
            return false;
        }
        return isSameTree(p->left,q->left)&&isSameTree(p->right,q->right);
    }
};
```

下面是用ai的

```
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    bool isSubtree(TreeNode* root, TreeNode* subRoot) {
        // 如果 root 已经走到头了，那肯定找不到了
        if (root == nullptr) {
            return false;
        }
        // subRoot 是空树的话，定义上是任何树的子树
        if (subRoot == nullptr) {
            return true;
        }

        // --- 这就是前序遍历思想的体现 ---
        
        // 1. "中"：判断以当前 root 为根的树，是否和 subRoot 相同
        bool isHere = isSameTree(root, subRoot);

        // 2. "左"：如果当前不是，就去左子树里找
        bool inLeft = isSubtree(root->left, subRoot);

        // 3. "右"：如果还不是，就去右子树里找
        bool inRight = isSubtree(root->right, subRoot);

        // 只要有一个地方找到了，结果就是 true
        return isHere || inLeft || inRight;
    }

    // 您写的 isSameTree 函数非常完美，可以直接用！
    bool isSameTree(TreeNode* p, TreeNode* q) {
        if (p == nullptr && q == nullptr) {
            return true;
        }
        if (p == nullptr || q == nullptr || p->val != q->val) {
            return false;
        }
        return isSameTree(p->left, q->left) && isSameTree(p->right, q->right);
    }
};

```