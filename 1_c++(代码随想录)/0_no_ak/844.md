```
class Solution {
public:
    bool backspaceCompare(string S, string T) {
        int i = S.length() - 1, j = T.length() - 1;
        int skipS = 0, skipT = 0;

        while (i >= 0 || j >= 0) {
            while (i >= 0) {
                if (S[i] == '#') {
                    skipS++, i--;
                } else if (skipS > 0) {
                    skipS--, i--;
                } else {
                    break;
                }
            }
            while (j >= 0) {
                if (T[j] == '#') {
                    skipT++, j--;
                } else if (skipT > 0) {
                    skipT--, j--;
                } else {
                    break;
                }
            }
            if (i >= 0 && j >= 0) {
                if (S[i] != T[j]) {
                    return false;
                }
            } else {
                if (i >= 0 || j >= 0) {
                    return false;
                }
            }
            i--, j--;
        }
        return true;
    }
};
```

没做出来。

这题思路是差不多的，但是代码没敲出来。就是在没结束的时候就一直判断两种状态，直到两种状态都没了就是当前值。

我自己构出来的代码逻辑很乱，第一次是对的，多重while，但是有个逻辑引入错了，应该是循环的时候在那里break，一直想着break和一步到位，第二次想要一次到位的一个逻辑，那个逻辑是对的，但是没有全局while去判断。

做双指针还是要想着一步步来，再复盘一下