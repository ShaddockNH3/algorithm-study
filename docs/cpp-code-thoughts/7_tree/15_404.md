# 404 左叶子之和

## 代码片段 1

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left),
 * right(right) {}
 * };
 */
class Solution {
public:
    int sumOfLeftLeaves(TreeNode* root) {
        if (root == nullptr ||
            root->left == nullptr && root->right == nullptr) {
            return 0;
        }
        int sum = 0;
        getSum(root, &sum);
        return sum;
    }
    void getSum(TreeNode* root, int* sum) {
        if (root == nullptr) {
            return ;
        }
        if (root->left != nullptr) {
            if (root->left->left == nullptr && root->left->right == nullptr) {
                *sum += root->left->val;
            }
        }
        getSum(root->left,sum);
        getSum(root->right,sum);
    }
};
```

## 复盘

主要就是搞清楚判断条件

还是这种好用，其实我有点搞不明白怎么不引用传参去写，让AI写一下看看——

## 代码片段 2

```cpp
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right)
 *         : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    int sumOfLeftLeaves(TreeNode* root) {
        // 1. 递归的终止条件
        // 如果是空节点，那它下面肯定没有左叶子，报告0
        if (root == nullptr) {
            return 0;
        }

        int midValue = 0; // 用来记录当前节点“亲自”发现的左叶子的值

        // 2. 判断当前节点的左孩子是不是一个叶子节点
        //    这是整个算法的核心！
        if (root->left != nullptr
            && root->left->left == nullptr
            && root->t->left->right == nullptr) {
            midValue = root->left->val;
        }

        // 3. 收集左右下属的报告，并汇总
        //    递归地去计算左子树中的左叶子之和
        int leftSum = sumOfLeftLeaves(root->left);
        //    递归地去计算右子树中的左叶子之和
        int rightSum = sumOfLeftLeaves(root->right);

        // 4. 向上级报告总结果
        //    总和 = 我亲自发现的 + 左边下属报告的 + 右边下属报告的
        return midValue + leftSum + rightSum;
    }
};
```
